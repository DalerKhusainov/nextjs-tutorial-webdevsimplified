/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: true,
  },
};

// FOR USING CREATING todo IN new/page.tsx
// serverActions: true,

module.exports = nextConfig;
